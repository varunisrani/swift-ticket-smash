# Spec Summary (Lite)

Implement a UI-only role-based dashboard system with three distinct user roles (Customer, Admin, Manager) that provides role-specific views of ticket data from Supabase. Features include a mock role switcher for testing, responsive dashboards tailored to each role's needs, and real-time data display from existing database without requiring authentication changes.